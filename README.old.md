# my-application

Dit is een applicatie in React

# Gebruik json-server:

open de terminal en type : Npx json-server --watch data/myData.json --port 8000 <enter>

## start de applicatie in een nieuw terminal : npm run start of npm start.
